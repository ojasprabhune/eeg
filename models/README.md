# Saved models

The `models` folder contains folders for notable Position LLM models and KMeans clustering models, clustered and trained on delta tokens and region tokens.

`delta_tokens` model is trained with scaled data (tokenized with delta tokenizer)
