[project]
name = "eeg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mediapipe>=0.10.14",
    "opencv-python>=4.12.0.88",
]

[tool.setuptools]
packages = ["eeg"]
