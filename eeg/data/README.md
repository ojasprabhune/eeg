# Data collection
`data_collection.py` outputs a CSV file where each row represents one time step and contains the 21 sets of X, Y, and Z positions per landmark (in sequential order).

Set the desired FPS in `data_collection.py`.